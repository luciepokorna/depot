<h1>Nová objednávka</h1>

<div class="depot_form" >
  <fieldset>
    <legend>Zadejte, prosím, své údaje.</legend>
    <%= render 'form' %>
  </fieldset>
</div>

<%= render 'form' %>

<%= link_to 'Back', orders_path %>
